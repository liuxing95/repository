# 场景 09 交付前审查

日期：2026-09-24。范围：当前分支中场景 09 的代码、迁移、测试与说明。按仓库 AGENTS.md 的子任务顺序执行约束，在主线程依次核对；本记录不冒充多代理独立结论。

| 视角 | 核对结论 |
|---|---|
| 正确性 | 日、任务实例、代次与投递键分开；计划改期和任务完成／删除会使旧排期失效。补发默认关闭，稍后提醒是显式例外。夏令时缺失时刻跳过、重复时刻取第一次。 |
| 可靠性 | 发送前写尝试，重启中断为未知；渠道结果与取消并发时保留真实尝试记录。主端释放前关闭规则并核对在途或未知结果。数据库和同目录栅栏一起回滚时，父目录锚点仍阻止旧发送。 |
| 安全与权限 | 提醒写接口沿用 Bearer、主端和策略版本；只有 macOS 本机渠道。AppleScript 使用固定脚本和参数，不插入任务正文、标题、令牌或来源内容。 |
| 迁移 | schema 8→9 在改表前创建 0600 快照；严格 schema 比较和恢复栅栏有测试。 |
| 可维护性与文档 | README、接手指南、场景方案、文档导航与场景 08 边界均同步；本地链接通过检查。 |

审查中已修复：重新配对后规则归属丢失（改为稳定设备 ID）；已发晨间规则重新登记可能重复（投递键按设备、类型、日期稳定）；改期与渠道调用并发时状态可能滞留发送中；结果未知的重发及主端交接边界；默认遗漏补发与稍后提醒冲突；旧数据库和同目录栅栏一起恢复时缺少外部锚点。

未解决的外部范围：P6 relay、真实手机到达／已读、整机旧快照回滚后的外部核对。这些不是本机模式已交付能力，方案 N3 保持未完成。当前未发现需留给下游修复的本机场景缺陷。最终验证数字见 `docs/implementation/reminder-delivery-control-validation.md`。
