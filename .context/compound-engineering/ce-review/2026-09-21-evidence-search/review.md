# 场景 03 代码审查

模式：`ce:review mode:autofix base:bacff36 plan:docs/plans/2026-09-21-004-feat-evidence-search-answer-plan.md`。覆盖该提交后的源码、测试、迁移、插件及文档，包括新增未跟踪文件。

按仓库的 Task/Subagent 工具映射在主线程顺序执行审查；没有独立子代理意见，不作跨审查者置信度加成。检查角度包括正确性、测试、可维护性、仓库约定、程序接口可用性、权限、性能、数据迁移、故障恢复、TypeScript 和界面异步状态。仓库没有 `docs/solutions/` 可复用历史结论。

## 已修复项

| 编号 | 位置 | 问题与修复 | 验证 |
|---|---|---|---|
| F1 | `search/search.ts` | 旧快照重新执行排名可能选到不同证据；现在固定证据 ID、范围与家族摘要，变化时拒绝继续使用 | 旧代切换、范围修改与快照断言 |
| F2 | `answers/evidence-pack.ts` | 派生页追溯原件时需要重新核对历史时点、集合和审核条件；否则后发原件可能进入历史答案 | 后见原件不能进入旧时点答案的回归用例 |
| F3 | `answers/answer.ts` | 模型引用校验应在作业成功前完成；否定被删或范围伪造时必须失败且不进入缓存 | 模型结构错误、遗漏否定、同范围分歧、实际预算记录 |
| F4 | `views/search.ts` | 旧权限轮询失败可能清掉后来的新查询；现在比较捕获的快照 ID | jsdom 异步轮询测试 |
| F5 | `connection.ts` | 新模型调用的客户端默认 10 秒早于提供方的 30 秒上限；改为明确的 35 秒，冷索引走有界长等待 | 有限超时测试、既有连接测试 |
| F6 | `ingestion/commit.ts` | 原件与解析接口也需执行显式本地 read 拒绝，不能只检查撤回 | HTTP 原件与解析拒绝用例 |
| F7 | `runtime/diagnostics.ts` | schema 升级后诊断支持版本需同步到 3 | 编译进程与真实桌面诊断 |
| F8 | `tests/faults/ingestion-commit.test.ts` | 真实进程完成断言使用默认 1 秒，完整回归出现仍为 running 的超时；显式使用 5 秒，不改生产 20 秒解析上限 | 定向链路与全量回归 |

## 需求核对与残余工作

| 范围 | 状态 | 说明 |
|---|---|---|
| R022—R028 / E1—E2 | 已实现 | 固定回读、范围、关系、家族、中文/别名/符号、完整索引代和过滤 |
| R029—R031 / E3 | 工程路径完成，效果验收未完成 | 本地原文整理可用；模型适配器通过确定性测试，不代表真实提供方或人工语义验收 |
| R031 / E4 知识检查 | 已实现当前登记对象范围 | 支持无来源、定位失效、覆盖、冲突、孤立及显式替代；未来 Wiki 页面扫描由场景 04 衔接 |
| R093 / E4 增强 | 保持关闭 | 固定题基线和门槛已实现；真实增强同题对照及约 80 题未实施 |
| A32 / A37 | 已实现并测试 | 撤回、read 拒绝、缓存与已知 ID、生成中撤回、候选保存再次校验 |
| A40 | 已实现当前已确认时间范围 | 未知时间、后发材料及派生页追溯的后发原件均不得冒充当时证据 |

| 优先级 | 未完成项 | 后续责任 |
|---|---|---|
| P1 | 指定真实模型、安装受信适配器并完成固定真实问题的人工作用条件核对；当前未满足重要主张支持率 ≥95% 的人工验收门槛 | 需要用户指定提供方/模型，并由后续实现与人工验收完成；不在本轮自动开启收费路线 |
| P2 | 7 个家族的 20 题基线不能代表大库效果，仍需扩展约 80 题；P7 选择一个增强再做同题对照 | 后续质量与增强阶段 |

迁移检查：只新增 schema 3 定义及相关版本路径，已知 v1/v2 的备份与重开验证覆盖；未知结构不写入。接口检查：插件动作均有本地 HTTP 入口；没有只供界面使用的私有捷径。权限检查：新正文输出均经过本地 read 检查，模型输出和保存候选复核固定证据。

结论：本地检索与证据整理可交付试点；不能声明场景 03 已完整通过模型效果验收。计划保持 active，模型与增强保持关闭。部署观察和回退限制见场景验收文档。

Review complete
