# 场景 05 完整顺序审查

日期：2026-09-22。基线 `139bd98`，分支 `codex/multi-source-ingestion`，范围为本轮场景 05 全部新增和修改文件。执行 `ce:review mode:autofix`，依据场景 05 方案及仓库约定，在主线程依次核对正确性、测试、维护、合同、权限、迁移、可靠性、异步界面和交付文档；没有独立子 agent 审查。仓库没有 `docs/solutions/` 可复用记录。

## 已修复并验证

1. 研究作业类型遗漏公共 JobInput 合同，首轮用例暴露后补齐。
2. 人工足够支持判断只核对问题范围，可能漏掉课题总范围。先补失败用例，再要求同时满足两层范围及必要证据类型。
3. 来源家族去重过粗会漏同一来源的不同原文；改为同家族、文本和范围同时一致才去重。
4. 库内快照和补采原先各算材料，可能绕过根上限；统一已用身份及预留名额，正式提交后转换预留，并测试全过程。
5. 修改表单后旧草案异步响应可能重新显示确认控件；用草案代号丢弃迟到响应，界面回归通过。
6. 长期排队的预算容器会阻碍主端交接；容器建立后完成，独立保存研究状态，子章仍共享同一根额度。
7. 完整报告写入需绑定冻结正文、最终快照、主张和证据清单，避免候选清单脱离正文；增加篡改清单的拒绝用例。
8. 模型返回 20 个未解决项时，再并入覆盖缺口会超出章节合同；二者分别保存和展示，增加上限回归。
9. 外部原文和用户输入在 Markdown 中以字面文本展示，UI 使用 textContent；不将来源陈述标为实际运行的实验结果。

## 审查结论与剩余门槛

库内原文研究流程可进入隔离试点。真实模型尚未安装；当前只允许完整原文主张，自由综合写作、提供方实际价格回执与人工语义质量未验收。RSH3 保持未完成，整个方案保持 active；不能作为完整研究模型能力发布。

检索为有上限的词法检索，人工覆盖判断不能由命中数替代；跨章节复杂矛盾仍需人工审阅。自动搜索发现不在当前实现中。取消保留费用且不重发未知请求；已导出的文件无法因权限撤回而远程删除。schema 5 回退不能覆盖升级后的权威账本。

## 验证

最终类型检查、lint 通过。`KB_TEST_OCI=1 KB_TEST_BUILT=1 pnpm test`：106 通过、2 跳过；`pnpm test:desktop`：21 项检查通过、页面错误 0。真实语料专项未执行，不改写场景 04 的历史失败记录。

细节和运行观察见[验收记录](../../../../docs/implementation/topic-research-validation.md)。
