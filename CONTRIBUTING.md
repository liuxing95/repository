# 开发与文档交付约定

首次开发先读 [开发者接手指南](docs/development/onboarding.md)。本仓库要求代码变更和相应说明在同一次交付中完成，让没有参与实现的人也能运行、理解和维护它。

## 文档放在哪里

| 读者要解决的问题 | 文档入口 | 何时更新 |
|---|---|---|
| 项目是什么，如何开始 | [README](README.md) | 能力范围、环境、安装或启动方式变化 |
| 刚接手如何跑通、读代码、调试 | [接手指南](docs/development/onboarding.md) | 流程、模块入口、数据位置或维护方式变化 |
| 为什么这样设计，场景如何拆分 | `docs/plans/` 中对应场景方案 | 设计决策、边界或实施范围变化；明确写出偏差 |
| 某场景怎么使用、调接口、验收和恢复 | `docs/implementation/` 中对应场景说明 | 每次功能交付或行为修复；篇幅过长时拆出使用说明并互相链接 |
| 新增资料从哪里进入 | [文档导航](docs/README.md) | 新增、移动或归档文档 |

纯内部重构或小修复可以只更新受影响章节及验证记录；如果确实没有使用或维护行为变化，在交付说明中写明理由。避免为了凑文档新建重复文件。原始调研和历史验收记录保留原日期、原结论，新增结果用新的日期或小节记录。

## 每次交付检查

- [ ] 说明本次解决的问题、已完成范围和仍未支持的情况。
- [ ] 提供前置条件、可执行步骤和预期结果；命令说明工作目录、变量、端口与数据路径。
- [ ] 对新增业务说明主流程、异常和恢复路径；跨模块流程补充或更新流程图。
- [ ] 标出相关源码入口；接口、配置、权限和数据结构有变化时同步说明。
- [ ] 记录实际验证命令、环境、结果与未覆盖范围。未运行的检查不能写成通过。
- [ ] 涉及存量数据时说明迁移、备份及回退限制；区分可重建产物和权威数据。
- [ ] 核对本地链接、按钮名称和示例；示例不用真实私密资料、令牌或密钥。
- [ ] 更新 README、接手指南和导航中受影响的内容，在最终交付中给出文档入口。

代码变更按接手指南选择测试，基本检查为 `pnpm check`；桌面、真实网络与 OCI 验证按影响范围增加。仅修改文档时，核对链接、命令与描述即可，不必重复整套业务测试。

## 新场景说明的最小结构

可以沿用以下结构，删去确实不适用的小节，并说明原因：

1. 本场景解决什么问题，当前完成到哪里。
2. 使用前需要什么，怎样完成一个最小例子，成功时看到什么。
3. 正常流程图，以及失败、取消、重试时的行为。
4. 相关代码入口、接口和配置，数据由谁保存、谁能修改。
5. 验证命令、实际结果、已知限制。
6. 排障、备份与恢复，关联设计和接手指南。

措辞以实际行为为准。例如“记录了索引请求”不能写成“已支持搜索”，“取得原件”不能写成“正式导入成功”。对外部资料的完整度、性能和安全边界，只写已验证的范围。

把交付约定放在根目录 `CONTRIBUTING.md`，参考 [GitHub 的仓库贡献指南说明](https://docs.github.com/en/communities/setting-up-your-project-for-healthy-contributions/setting-guidelines-for-repository-contributors)。
