# 本次整理检查记录

日期：2026-09-21。此记录只描述合集整理，不继承历史材料中的测试结果作为本轮结果。

## 本轮已执行

| 检查 | 结果 |
|---|---|
| 原始交付覆盖 | 12 个记录：9 份 Markdown、3 个 ZIP |
| 原始 Markdown 字节 | 9 份与当前提供文件逐字节相同 |
| 原始 ZIP 完整性与成员 | CRC 检查通过，54 个成员全部展开并逐字节相同 |
| 新增整理文档相对链接 | 486 条检查，0 条缺失 |
| 新增整理文档代码围栏 | 配对检查通过 |
| JSON/JSONL 语法 | 当时已有 6 个 JSON、16 条 JSONL 记录通过数据解析 |
| 来源索引 | 98 个精确 URL 去重项，关联的包内文件全部存在 |
| 原始实施包 SHA256SUMS | 41 条原校验值通过核对，未运行其检查脚本 |
| 归档结构 | 无符号链接、字体、可执行二进制、凭据文件或嵌套 ZIP |

逐项记录见 `packaging-checks.json`。最终载荷清单见 `manifest.json`；`SHA256SUMS` 额外包含 manifest 自身。

## 本轮没有执行

没有联网重查源页面，没有安装依赖、运行旧示例的系统测试、调用模型、执行 Obsidian/TaskNotes 集成、处理真实语料、配置日历或创建提醒。历史检查文档按原样保留，只属于其原日期、原文件和原验证范围。

没有重写原始规范，整理后的总览与学习方式文件只是归纳；对缺少完整论文身份的历史对话线索明确保留不确定性，没有补造来源。

## 哈希检查的局限

内容哈希用于核对包内文件是否变化，不证明技术结论正确，也不是数字签名。读者修改模板或文档之后出现不匹配属于正常变化，不能继续使用原校验结果声明未变。
