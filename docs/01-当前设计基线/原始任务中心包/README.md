# Obsidian 知识库 v2.1 方案包

先阅读主报告，再阅读任务中心实施起步清单。两份文档均为待实施设计；不是已经安装的软件。

`planning-policy.example.json` 是本项目未来配置示例，不能直接导入 TaskNotes；默认没有可用时间、关闭提醒、不自动移动任务。

`VALIDATION.md` 只记录文档、配置和虚构日程的检查，不是插件/服务测试报告。

本轮没有改动旧方案或持久 Library 文件，没有读取或修改真实日历、Todoist 待办或 Obsidian Vault，没有建立提醒。
