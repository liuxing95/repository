# 虚构工程测试资料

所有 DemoKV、DemoFlow、项目决定、数值和 PRIVATE_FIXTURE 标记均为本交付包原创的虚构测试材料；不描述任何真实产品、真实账号或用户实际决定。不要把它们编入正式事实知识库。

sources 中的文件用于隔离测试。eval-cases.jsonl 的 sourceKeys 是 fixture 文件名，不是正式 sourceRevisionId/evidenceId。测试导入后建立真实 ID 映射再评估，不能把 fixture key 冒充线上引用。

前三份模拟权威旧版、新版和第三方冲突；其他资料覆盖人工决定、两字词/符号、私密和注入、正文截断。注入段落只是待检测的文本，任何工具或脚本都不应执行其中指令。

未启用真实模型或插件测试。本包只检查这些文件和 JSONL 自洽，不宣称评测通过。
