# 交付检查记录

**日期：2026-09-20｜范围：设计文档、人工模板与参考合同，不是已实现系统验收**

## 本次实际执行

运行 `python checks/verify_design.py`，退出码 0。该脚本只读检查本包文件，在内存 SQLite 执行 DDL 和约束样例，并对参考 TS 接口进行 noEmit 类型检查；不访问真实 Vault，不安装依赖，不调用模型。

共 **28 项局部检查**通过：两份 SQL 草案成功创建；主要外键、hash、修订去重、证据范围、补丁动作、批准摘要绑定、单 active writer 行约束及冲突回执通过样例验证；预算条件 UPDATE 的顺序预占样例通过。

FTS 测试使用事先明确分隔的词项“权限、重排、知识”；精确表测试 C++、Node.js 和 app.vault.process。**这不等于真实 tokenizer、中文召回率、并发预算或服务性能测试已经通过。**

配置可解析，未初始化、模型关闭、所有写入关闭；16 个 synthetic JSONL 用例 ID 唯一，所引 8 份来源文件存在；文档围栏、来源引用定义、包内相对链接和 starter-vault 内部链接检查通过；E00–E17 任务存在。

## 实际检查环境

Node v22.16.0；TypeScript Version 5.8.3；Python 内置 SQLite 3.46.1。

参考合同的检查命令：

```bash
tsc --strict --noEmit --target ES2022 --module NodeNext \
  --moduleResolution NodeNext references/contracts.ts
```

**当前检查容器使用 Node 22，不是设计选择的 Node 24 部署环境。** 这里只验证类型定义和内存 DDL，不证明 Node 24 的应用、native 驱动或 Obsidian 插件兼容性。目标环境必须在实施阶段重新测试。

## 本轮自审后的修正

来源导入与 Wiki 改写拆成独立开关；SourceRevision 与 ParseArtifact 使用一致字段；冲突回执允许只记录第三种 hash，而不要求复制私密人工正文；明确 epoch 不等于磁盘栅栏，未知在途写入必须先 reconciliation；明确数据库一致备份不等于整个 Vault 的原子快照；补充 20 题的真实评分分母。

## 未执行，也不宣称完成

未实现或启动本地服务、CLI、插件、预算网关、编译器适配器或 MCP；未安装上游 SDK；未访问用户的真实 Vault；未调用真实模型；未跑 E00–E14 应用测试、真实故障恢复、备份或跨设备同步；未测准确率、性能、账单和工时。

包内 starter-vault 是普通 Markdown 模板，可人工使用；其他自动化能力必须按计划开发。SHA256SUMS 和 ZIP 完整性检查只用于交付文件一致性，不是代码签名或软件安全认证。
