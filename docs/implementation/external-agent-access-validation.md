# 场景 11 验证记录

核对日期：2026-09-24。对应[使用与维护说明](external-agent-access.md)和[场景方案](../plans/2026-09-21-012-feat-external-agent-access-plan.md)。

| 检查 | 结果 |
|---|---|
| `pnpm check`：TypeScript、ESLint、构建、全量单测、构建产物检查 | 通过；常规测试 186 通过、6 条条件测试跳过，构建产物测试 2 通过 |
| 本机 HTTP 客户端授权与固定来源过滤 | 自动化测试通过；未授权来源及追溯到未授权原始来源的摘要正文不返回 |
| 同一请求重试、不同载荷冲突、一次性密钥 | 自动化测试通过；密钥只在首个创建响应出现 |
| 模型调用在途撤销与重复请求 | 使用受信测试提供方通过；仅派发一次，撤销后正文不交付 |
| MCP stdio 子进程与真实 JSON-RPC 客户端 | 自动化握手、工具列表、搜索、固定证据回读、未知工具和任意路径拒绝通过 |
| `pnpm test:desktop`：Obsidian 1.13.7 独立试点 | 通过；配对、主端、设置、作业、诊断、释放和撤销仍可用；新界面可见并留有本机截图 `.context/runtime-validation/obsidian-agent-access.png` |
| 本次改动文档的本地链接 | 检查通过，未发现缺失目标 |

自动化测试使用虚构资料和测试提供方。它不能证明真实模型语义质量、第三方桌面客户端兼容性、跨平台系统凭据库操作或外部 Agent 收到正文后的行为。当前仓库不安装真实模型适配器，模型授权创建后的问答仍会以 `UNAVAILABLE` 结束；已派发费用的重试行为由预算账本测试和本场景在途测试共同约束。

本次审查沿着密钥创建、普通 HTTP 会话隔离、来源过滤、派生证据追溯、在途撤销、重复请求和费用账本逐项回看；修复了派生证据可能追溯到客户端范围外原始来源的问题。没有发现仍会绕过只读工具白名单的入口。MCP 客户端测试为真实 stdio 子进程加 JSON-RPC 测试客户端，不等同于对所有第三方桌面产品的人工兼容认证。
